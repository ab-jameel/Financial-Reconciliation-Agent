[project]
name = "financial-reconciliation-agent"
version = "0.1.0"
requires-python = ">=3.11"

[tool.uv.workspace]
members = ["backend/common", "backend/orchestration", "backend/erp"]

[dependency-groups]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "faker>=26.0",
    "python-dotenv>=1.0"
]